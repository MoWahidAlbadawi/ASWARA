import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette : {
      primary: {
        main: '#D4AF37',
        light: '#F1E5AC',
        dark: '#A88629', 
    },
      background: {
      default: '#f5f5f5', 
    },
    }
})