import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette : {
      primary: {
        main: '#D4AF37',
        light: '#F1E5AC',
        dark: '#A88629', 
    },
    secondary : {
      main : '#4B2E00'
    },
      background: {
      default: '#f5f5f5', 
    },
    }
})